# Google OAuth Client ID (Web Application type)
# Create at: https://console.cloud.google.com → APIs & Services → Credentials
# Authorized redirect URIs must include:
#   http://localhost:5173/oauth-callback
#   https://your-domain.pages.dev/oauth-callback
#   com.myspace.app:/oauth-callback  (mobile deep link)
VITE_GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com

# Google OAuth Client Secret (required for Web Application type token exchange)
# Found in Google Cloud Console → APIs & Services → Credentials → click your OAuth client
# Note: this is bundled into the JS — acceptable for a personal PWA with no backend
VITE_GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret